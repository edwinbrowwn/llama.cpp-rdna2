Implement a bounded LRU cache for a concurrent HTTP service. Explain locking, eviction, metrics, and tests.
Find the race condition in a producer consumer queue and propose a fix with a deterministic stress test.
Design a parser for a small programming language with error recovery and source locations.
Optimize a matrix multiplication kernel for cache locality and explain the benchmark methodology.
Write a PostgreSQL migration that adds a unique constraint without blocking production traffic.
Review this authentication flow for replay attacks, confused deputy bugs, and missing authorization checks.
Implement a streaming JSON decoder that handles partial UTF-8 sequences across input chunks.
Compare a trie, radix tree, and hash table for autocomplete with one million keys.
Design a retry policy for an idempotent RPC client with deadlines and jitter.
Explain how to debug a memory leak in a C++ service using heap snapshots and sanitizers.
Implement a lock-free single producer single consumer ring buffer and list its memory ordering assumptions.
Design a content-addressed artifact store with atomic publication and garbage collection.
Write property-based tests for a parser and identify useful invariants.
Optimize a Python data pipeline that spends most of its time serializing objects.
Explain how a compiler should lower closures and captured variables to a stack and heap representation.
Implement a safe bounded thread pool with cancellation and graceful shutdown.
Review a Kubernetes deployment for readiness probes, resource limits, and rolling update failures.
Design an incremental build system that invalidates only affected targets.
Explain how to diagnose false sharing in a multithreaded numerical workload.
Write a fuzzing strategy for a binary protocol decoder and define crash triage rules.
